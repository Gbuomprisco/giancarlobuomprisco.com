---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-muted text-sm whitespace-nowrap">
  {date.toLocaleDateString('en-us', { month: 'short', day: 'numeric' })}
</time>
