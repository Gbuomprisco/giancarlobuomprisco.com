---
pagination:
  data: collections.posts
  size: 50
  alias: list
  reverse: true
permalink: '<% if (pagination.pageNumber > 0) { %><%= `page${pagination.pageNumber + 1}/index.html` %><% } else { %>index.html<% } %>'
eleventyComputed:
  title: '<% if (pagination.pageNumber > 0) { %><%= `Page ${pagination.pageNumber + 1} | ${site.title}` %><% } else { %><%= site.title %><% } %>'
---

<div class="container mx-auto">
  <%- include('_includes/layouts/header.ejs') %>

  <div>
    <div class="pt-10 flex items-center">
      <div class="mr-8">
        <img class="avatar" src="/assets/images/giancarlo.jpeg" />
      </div>

      <div>
        <h1 class="font-bold text-5xl">
          Hey, I'm Giancarlo
        </h1>

        <h2 class="text-xl text-grey-800">
          I'm a Software Consultant, Blogger & aspiring Indie Hacker.
        </h2>

        <h3 class="text-grey-800">
          I write decent UIs, and help people learn how to do it, too.
        </h3>
      </div>
    </div>
  </div>

  <div class="mt-12">
    <h4 class="font-bold text-lg">
      About Me
    </h4>

    <ul>
      <li>I write on <a href="https://medium.com/@.gc">Medium</a> - mostly long-form posts about Software Development</li>
      <li>I write on <a href="https://angularbites.com">Angular Bites</a> - a list of short-form thoughts about Angular and its ecosystem</li>
      <li>I'm a co-founder of Formtoro, a data-collection platform for businesses. Not publicly launched yet.</li>
      <li>My passion is coding, but I'm not <b>that</b> boring. I love going to the gym, too.</li>
    </ul>
  </div>

  <div class="mt-6">
    <h4 class="font-bold text-lg">
      Let's Connect
    </h4>

    <ul>
      <li>
        Follow me on <a target="_blank" href="https://twitter.com/gc_psk">Twitter</a>
      </li>

      <li>
        Connect on <a target="_blank" href="https://www.linkedin.com/in/giancarlobuomprisco/">Linkedin</a>
      </li>

      <li>
        Fork my code on <a target="_blank" href="https://github.com/gbuomprisco">Github</a>
      </li>
    </ul>
  </div>

  <div class="mt-6">
    <h4 class="font-bold text-lg">
      Blog
    </h4>

    <ul>
      <% list.forEach((post) => { %>
        <li>
          <a href="<%= post.url %>"><%= post.data.title %></a>
          - <%= this.dateFns.format(new Date(post.data.date), 'LLLL d, yyyy') %>
        </li>
      <% }) %>
    </ul>
  </div>

  <div class="pt-3">
    <% if (pagination.previous) { %>
      <a href="<%= pagination.previous.replace(/index.html$/, "") %>">Newer Posts</a>
    <% } %> <% if (pagination.next) { %>
      <a href="<%= pagination.next.replace(/index.html$/, "") %>">Older Posts</a>
    <% } %>
  </div>
</div>
