---
label: Collection
hide_body: false
fields:
- name: name
  type: text
  config:
    required: true
  label: Name
- name: primaryColor
  type: color
  label: Primary Color
  config:
    required: false
    color_format: Hex
- name: logo
  type: file
  config:
    maxSize: 64
  label: Logo
- name: emoji
  type: text
  config:
    required: false
  label: Emoji
- name: primaryColorLight
  type: color
  label: Primary Color Light
  config:
    required: false
    color_format: Hex
- name: contrastColor
  type: color
  label: Contrast Color
  config:
    required: false
    color_format: Hex
